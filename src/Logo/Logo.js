import { Link } from "react-router-dom";
import "./Logo.css";

const Logo = (props) => {
  return (
    <div className="home_b">
      <Link to="/">
    
        <span className="span">TIMART </span>
        </Link>
      </div>

  );
};

export default Logo;
